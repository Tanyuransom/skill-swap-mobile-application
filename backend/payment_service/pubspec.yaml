name: payment_service
description: Payment Service for SkillSwapp - Handles mobile money payments (Orange Money, MTN MoMo)
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dotenv: ^4.0.0
  http: ^1.1.0  # For mobile money API calls
  
  # Shared package
  skillswapp_shared:
    path: ../shared

dev_dependencies:
  lints: ^2.0.0
