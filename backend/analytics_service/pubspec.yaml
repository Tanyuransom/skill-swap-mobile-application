name: analytics_service
description: Analytics Service for SkillSwapp - Track content consumption and calculate tutor earnings
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dotenv: ^4.0.0
  
  skillswapp_shared:
    path: ../shared

dev_dependencies:
  lints: ^2.0.0
