name: review_service
description: specialized service for managing course reviews and ratings.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  postgres: ^2.6.1
  args: ^2.4.2
  image: ^4.0.17  # For image processing
  skillswapp_shared:
    path: ../shared

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
  http: ^1.1.0
