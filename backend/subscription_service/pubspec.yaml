name: subscription_service
description: Subscription Service for SkillSwapp - Premium membership (2,500 XAF/month)
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dotenv: ^4.0.0
  http: ^1.1.0
  
  skillswapp_shared:
    path: ../shared

dev_dependencies:
  lints: ^2.0.0
